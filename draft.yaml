models:
  Package:
    vendor: string
    package: string
    repository_url: string
    license: string nullable
    package_url: string

  PackageVersion:
    name: string
    dist_url: string
    dist_type: string
    json_created_at: datetime nullable

